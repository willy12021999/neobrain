<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NEOBRAIN</title>
    <style>
        body {
            margin: 0;
            font-family: 'Georgia', serif;
            background-color: #f7f2eb;
            color: #fff;
        }

        header {
            background-color: #6b5750;
            padding: 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .logo {
            height: 60px;
        }

        .navbar {
            display: flex;
            gap: 20px;
        }

        .navbar a {
            color: #fff;
            text-decoration: none;
            font-size: 14px;
            text-transform: uppercase;
        }

        .title {
            font-size: 28px;
            font-weight: bold;
        }

        .main {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 40px;
            padding: 60px 20px;
        }

        .card {
            background-color: #6b5750;
            padding: 60px;
            border-radius: 20px;
            width: 300px;
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            transition: transform 0.3s;
        }

        .card:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <header>
        <div class="navbar">
            <a href="#">Home</a>
            <a href="#">Presentation</a>
            <a href="#">Contacte</a>
            <a href="#">Portfolio</a>
        </div>
        <div class="title">NEOBRAIN</div>
        <img src="logo.png" alt="Logo Neobrain" class="logo">
    </header>

    <section class="main">
        <div class="card">Espace etudiant</div>
        <div class="card">Espace Enseignant</div>
    </section>
</body>
</html>
